#include <iostream>
using namespace std;

// 定义加法函数
int add(int x, int y) {  
    return x + y; 
}

int main() {
    int a, b;
    cout << "Enter two numbers: ";
    cin >> a >> b;           // 接收用户输入
    int sum = add(a, b);     // 调用函数
    cout << "Sum: " << sum << endl; // 输出结果
    return 0;
}
